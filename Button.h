#pragma once
#include "init.h"
#include "Img.h"
#include "Text.h"

class Button{
private:
    SDL_Rect dscButton;
    bool checked(toado);
public:
    void setup(const SDL_Rect&);
    bool click(toado,toado);
};
